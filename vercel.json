{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "pnpm install",
        "buildCommand": "pnpm run build"
      }
    }
  ],
  "build": {
    "env": {
      "VERCEL_BUILD_COMMAND": "pnpm run build"
    }
  }
} 